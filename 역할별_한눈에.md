# 역할별 한눈에 보기

이 파일만 읽어도 “나는 무엇을 하면 되는가”가 보이도록 정리했습니다.
실행 명령의 상세는 각 더미 폴더의 `README.md`와 `담당자_가이드.md`에 있습니다.

---

## Docker 담당

**폴더:** `01-dummy-docker`

**지금 할 수 있는 것**

```bash
# 임시 MySQL
docker run --name studyboard-mysql \
  -e MYSQL_ROOT_PASSWORD=rootpass \
  -e MYSQL_DATABASE=studyboard \
  -e MYSQL_USER=study \
  -e MYSQL_PASSWORD=study123 \
  -p 3306:3306 -d mysql:8.0

# 백엔드
cd 01-dummy-docker/backend
cp .env.example .env
npm install && npm run start:dev

# 프론트 (다른 터미널)
cd 01-dummy-docker/frontend
cp .env.example .env.local
npm install && npm run dev
```

브라우저: `http://localhost:3000`

**당신이 더 해야 하는 것**

- Next.js / NestJS Dockerfile
- MySQL을 compose 서비스로
- `docker-compose.yml` + Docker Network
- 환경변수 (`DB_HOST=database`)
- `docker compose up -d` 로 전부 기동

**완료 확인**

```bash
docker compose ps
curl http://localhost:4000/health
docker network inspect 01-dummy-docker_app-network
```

모임 당일: 실제 프로젝트에 같은 Docker를 적용하고, 결과를 Nginx 담당에게 넘깁니다.

---

## Nginx 담당

**폴더:** `02-dummy-nginx`

**지금 할 수 있는 것**

```bash
cd 02-dummy-nginx
docker compose up -d --build
curl http://localhost:3000
curl http://localhost:4000/api/health
```

**당신이 더 해야 하는 것**

- Nginx 컨테이너 추가
- `upstream` / `location` / `proxy_pass`
- ` / ` → frontend:3000
- ` /api/ ` → backend:4000
- access.log, error.log
- 컨테이너 안에서 `curl backend:4000/health`

**완료 확인**

```bash
curl localhost
curl localhost/api/health
docker compose exec nginx curl -s http://backend:4000/health
```

모임 당일: Docker 담당자가 방금 만든 실제 프로젝트에 Nginx를 추가합니다.

---

## AWS 배포 담당

**폴더:** `03-dummy-aws-deploy`

**지금 로컬에서 할 수 있는 것**

```bash
cd 03-dummy-aws-deploy
docker compose up -d --build
curl http://localhost
curl http://localhost/api/health
```

브라우저: `http://localhost`  (포트 80)

**당신이 더 해야 하는 것**

- EC2 생성, SSH
- EC2에 Docker 설치
- 이 프로젝트 전달
- `docker compose up -d --build`
- EC2에서 `curl localhost`
- 본인 PC에서 `http://EC2_PUBLIC_IP`

**완료 확인**

```bash
# EC2
curl localhost
curl localhost/api/health
# PC 브라우저
http://EC2_PUBLIC_IP
```

모임 당일: Nginx까지 적용된 실제 프로젝트를 EC2에 올리고, 그 인스턴스를 Network 담당에게 넘깁니다.

---

## AWS Network 담당

**폴더:** `04-dummy-aws-network`

**지금 할 수 있는 것**

- 로컬 또는 EC2에서 03과 동일하게 앱 기동
- `/health` 로 ALB 헬스체크 가능

```bash
cd 04-dummy-aws-network
docker compose up -d --build
curl http://localhost/health
```

**당신이 더 해야 하는 것**

- VPC, Subnet, Route Table, IGW
- Security Group (ALB / EC2 분리)
- ALB, Target Group, Health Check `/health`
- 인터넷 → ALB → EC2:80 → Nginx 흐름
- (선택) Route53, HTTPS, RDS

**완료 확인**

```text
Target Group healthy
curl ALB_DNS 성공
curl ALB_DNS/api/health 성공
EC2 공인 IP 직접 80은 SG로 차단
```

모임 당일: 이미 떠 있는 실제 EC2에 네트워크만 연결합니다. 앱을 다시 만들지 않습니다.

---

## 모임 당일 안전장치

앞 단계가 막히면:

```text
backup/docker-completed
backup/nginx-completed
backup/deploy-ready
```

정상본으로 전환한 뒤 다음 발표를 이어갑니다.
